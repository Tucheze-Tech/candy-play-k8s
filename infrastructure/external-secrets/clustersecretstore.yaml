apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-ksa
  namespace: external-secrets
  annotations:
    iam.gke.io/gcp-service-account: "external-secrets-sa@candy-play.iam.gserviceaccount.com"
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-store
spec:
  provider:
    gcpsm:
      projectID: candy-play
      auth:
        workloadIdentity:
          clusterLocation: europe-west3
          clusterName: candyplay-prod
          clusterProjectID: candy-play
          serviceAccountRef:
            name: external-secrets-ksa
            namespace: external-secrets
