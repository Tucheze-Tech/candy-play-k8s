apiVersion: v1
kind: Service
metadata:
  name: {{ include "gamble-hub.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gamble-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gamble-hub.selectorLabels" . | nindent 4 }}
