{{/*
Naming + label helpers now live in the candy-common library chart
(charts/candy-common/templates/_helpers.tpl): candy-common.fullname,
candy-common.labels, candy-common.selectorLabels. This file is intentionally
left as a placeholder so the chart structure stays obvious.
*/}}
