apiVersion: v1
kind: Service
metadata:
  name: {{ include "icore.fullname" . }}
  namespace: candy-services
  labels:
    {{- include "icore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "icore.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
